#!/bin/bash
# ╔═══════════════════════════════════════════════════════════════════════════════╗
# ║                    🌾 Field Suite Pro - Project Generator                      ║
# ║                         النسخة الاحترافية الكاملة                              ║
# ╠═══════════════════════════════════════════════════════════════════════════════╣
# ║  يتضمن:                                                                       ║
# ║  ✅ JWT Authentication حقيقي                                                  ║
# ║  ✅ Alembic Database Migrations                                               ║
# ║  ✅ Celery Background Tasks                                                   ║
# ║  ✅ Redis Caching                                                             ║
# ║  ✅ Prometheus Metrics                                                        ║
# ║  ✅ WebSocket Support                                                         ║
# ║  ✅ File Upload Validation                                                    ║
# ║  ✅ Unit + Integration Tests                                                  ║
# ║  ✅ GitHub Actions CI/CD                                                      ║
# ║  ✅ React + TypeScript + Tailwind                                             ║
# ║  ✅ i18n (العربية/English)                                                    ║
# ║  ✅ PWA Support                                                               ║
# ╚═══════════════════════════════════════════════════════════════════════════════╝

set -e

# ═══════════════════════════════════════════════════════════════════════════════
# المتغيرات والألوان
# ═══════════════════════════════════════════════════════════════════════════════
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
WHITE='\033[1;37m'
NC='\033[0m'
BOLD='\033[1m'

PROJECT_NAME="${1:-field_suite_pro}"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# ═══════════════════════════════════════════════════════════════════════════════
# الدوال المساعدة
# ═══════════════════════════════════════════════════════════════════════════════

print_banner() {
    echo -e "${GREEN}"
    echo "╔═══════════════════════════════════════════════════════════════╗"
    echo "║           🌾 Field Suite Pro - Generator v2.0                 ║"
    echo "║              نظام إدارة الحقول الزراعية المتقدم               ║"
    echo "╚═══════════════════════════════════════════════════════════════╝"
    echo -e "${NC}"
}

log_info() {
    echo -e "${BLUE}ℹ️  $1${NC}"
}

log_success() {
    echo -e "${GREEN}✅ $1${NC}"
}

log_warning() {
    echo -e "${YELLOW}⚠️  $1${NC}"
}

log_error() {
    echo -e "${RED}❌ $1${NC}"
}

log_step() {
    echo -e "\n${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
    echo -e "${CYAN}${BOLD}📦 $1${NC}"
    echo -e "${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}\n"
}

write_file() {
    local file_path="$1"
    local content="$2"
    mkdir -p "$(dirname "$file_path")"
    cat > "$file_path" << 'CONTENT_EOF'
$content
CONTENT_EOF
    echo -e "${GREEN}  ✓ ${file_path}${NC}"
}

# دالة محسنة لكتابة الملفات
create_file() {
    local file_path="$1"
    shift
    mkdir -p "$(dirname "$file_path")"
    cat > "$file_path"
    echo -e "${GREEN}  ✓ ${file_path}${NC}"
}

# ═══════════════════════════════════════════════════════════════════════════════
# البداية
# ═══════════════════════════════════════════════════════════════════════════════

print_banner
log_info "اسم المشروع: ${WHITE}${BOLD}$PROJECT_NAME${NC}"
log_info "المسار: ${WHITE}$(pwd)/$PROJECT_NAME${NC}"
echo ""

# التحقق من المتطلبات
log_step "التحقق من المتطلبات"

command -v docker >/dev/null 2>&1 || { log_error "Docker غير مثبت"; exit 1; }
command -v docker-compose >/dev/null 2>&1 || command -v "docker compose" >/dev/null 2>&1 || { log_error "Docker Compose غير مثبت"; exit 1; }

log_success "جميع المتطلبات متوفرة"

# ═══════════════════════════════════════════════════════════════════════════════
# إنشاء هيكل المجلدات
# ═══════════════════════════════════════════════════════════════════════════════

log_step "إنشاء هيكل المجلدات"

mkdir -p "$PROJECT_NAME"/{backend/app/{api/v1/endpoints,core,db,models,schemas,services,repositories,tasks,utils,middleware},backend/alembic/versions,backend/tests/{unit,integration,e2e,fixtures},web/src/{api,components/{ui,advisor,fields,ndvi,auth,common,layout},hooks,pages,stores,types,utils,i18n,assets},scripts,docs,infra/{docker,k8s,terraform},.github/workflows}

log_success "تم إنشاء هيكل المجلدات"

# ═══════════════════════════════════════════════════════════════════════════════
# استيراد ملفات التكوين
# ═══════════════════════════════════════════════════════════════════════════════

log_step "إنشاء ملفات التكوين الأساسية"

# تحميل الأجزاء
source "$SCRIPT_DIR/parts/01_backend_core.sh"
source "$SCRIPT_DIR/parts/02_backend_auth.sh"
source "$SCRIPT_DIR/parts/03_backend_models.sh"
source "$SCRIPT_DIR/parts/04_backend_services.sh"
source "$SCRIPT_DIR/parts/05_backend_api.sh"
source "$SCRIPT_DIR/parts/06_backend_tasks.sh"
source "$SCRIPT_DIR/parts/07_backend_tests.sh"
source "$SCRIPT_DIR/parts/08_frontend_core.sh"
source "$SCRIPT_DIR/parts/09_frontend_components.sh"
source "$SCRIPT_DIR/parts/10_docker_infra.sh"
source "$SCRIPT_DIR/parts/11_cicd.sh"

# ═══════════════════════════════════════════════════════════════════════════════
# النهاية
# ═══════════════════════════════════════════════════════════════════════════════

log_step "اكتمل إنشاء المشروع"

echo -e "${GREEN}"
echo "╔═══════════════════════════════════════════════════════════════╗"
echo "║                    ✅ تم بنجاح!                               ║"
echo "╠═══════════════════════════════════════════════════════════════╣"
echo "║  📁 المشروع: $PROJECT_NAME"
echo "║                                                               ║"
echo "║  🚀 للتشغيل:                                                  ║"
echo "║     cd $PROJECT_NAME"
echo "║     ./scripts/start.sh                                        ║"
echo "║                                                               ║"
echo "║  🌐 الروابط:                                                  ║"
echo "║     Frontend:  http://localhost:3000                          ║"
echo "║     API Docs:  http://localhost:8000/docs                     ║"
echo "║     Flower:    http://localhost:5555                          ║"
echo "║     Metrics:   http://localhost:8000/metrics                  ║"
echo "╚═══════════════════════════════════════════════════════════════╝"
echo -e "${NC}"
