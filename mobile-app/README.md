# 📱 Sahool Mobile App

تطبيق الموبايل لمنصة Sahool الزراعية الذكية

## 🚀 التقنيات المستخدمة

- **React Native** - إطار العمل الأساسي
- **Expo** - أدوات التطوير والنشر
- **TypeScript** - للـ Type Safety
- **React Navigation** - للتنقل بين الشاشات
- **React Native Paper** - مكتبة UI Components
- **Axios** - للاتصال بالـ API
- **React Native Maps** - للخرائط التفاعلية

## 📋 المتطلبات

- Node.js >= 16
- npm أو yarn
- Expo CLI
- Android Studio (للتطوير على Android)
- Xcode (للتطوير على iOS - Mac فقط)

## 🛠️ التثبيت

```bash
# تثبيت المكتبات
npm install

# أو باستخدام yarn
yarn install
```

## 🏃 التشغيل

### Development Mode

```bash
# تشغيل Expo
npm start

# تشغيل على Android
npm run android

# تشغيل على iOS
npm run ios

# تشغيل على المتصفح
npm run web
```

### مسح الـ Expo على هاتفك

1. حمّل تطبيق **Expo Go** من App Store أو Google Play
2. افتح التطبيق واضغط على "Scan QR Code"
3. امسح الـ QR Code الذي يظهر في Terminal

## 📱 الشاشات

### 1. Home Screen (الشاشة الرئيسية)
- إحصائيات سريعة (عدد الحقول، المساحة، NDVI)
- حالة الطقس
- التنبيهات النشطة
- إجراءات سريعة

### 2. Fields Screen (شاشة الحقول)
- قائمة جميع الحقول
- عرض على الخريطة
- بحث وفلترة
- إضافة حقل جديد

### 3. Field Detail Screen (تفاصيل الحقل)
- معلومات الحقل
- خريطة الحدود
- بيانات NDVI الحالية
- توقعات الطقس
- بيانات التربة
- نصائح AI

### 4. NDVI Screen (شاشة NDVI)
- صور NDVI الحديثة
- الرسوم البيانية التاريخية
- تحليل صحة النباتات
- مقارنة بين الحقول

### 5. Alerts Screen (شاشة التنبيهات)
- التنبيهات النشطة
- سجل التنبيهات
- تصنيف حسب الأولوية
- إشعارات Push

### 6. Profile Screen (شاشة الملف الشخصي)
- معلومات المستخدم
- الإعدادات
- تغيير اللغة
- تسجيل الخروج

## 🔧 التكوين

### متغيرات البيئة

أنشئ ملف `.env` في المجلد الرئيسي:

```env
EXPO_PUBLIC_API_URL=http://your-api-url.com
EXPO_PUBLIC_GOOGLE_MAPS_KEY=your_google_maps_key
```

### API Endpoints

التطبيق يتصل بالـ API التالية:

- `POST /api/v1/auth/login` - تسجيل الدخول
- `GET /api/v1/dashboard` - بيانات الشاشة الرئيسية
- `GET /api/v1/fields` - قائمة الحقول
- `GET /api/v1/fields/{id}` - تفاصيل حقل
- `GET /api/v1/imagery/fields/{id}/ndvi-latest` - آخر NDVI
- `GET /api/v1/alerts` - التنبيهات
- `GET /api/v1/weather/field/{id}` - الطقس
- `GET /api/v1/agent/field/{id}/advice` - نصائح AI

## 🎨 التصميم

التطبيق يستخدم نظام ألوان مستوحى من الطبيعة:

- **Primary:** #2E7D32 (أخضر زراعي)
- **Secondary:** #1976D2 (أزرق سماوي)
- **Accent:** #FFA726 (برتقالي دافئ)
- **Error:** #D32F2F (أحمر تحذيري)

## 📦 البناء للإنتاج

### Android

```bash
# بناء APK
expo build:android -t apk

# بناء AAB (للنشر على Google Play)
expo build:android -t app-bundle
```

### iOS

```bash
# بناء IPA
expo build:ios
```

## 🧪 الاختبار

```bash
# تشغيل الاختبارات
npm test

# تشغيل مع Coverage
npm test -- --coverage
```

## 📝 هيكل المشروع

```
mobile-app/
├── src/
│   ├── screens/          # شاشات التطبيق
│   │   ├── HomeScreen.tsx
│   │   ├── FieldsScreen.tsx
│   │   ├── FieldDetailScreen.tsx
│   │   ├── NDVIScreen.tsx
│   │   ├── AlertsScreen.tsx
│   │   └── ProfileScreen.tsx
│   ├── components/       # مكونات قابلة لإعادة الاستخدام
│   │   ├── FieldCard.tsx
│   │   ├── NDVIChart.tsx
│   │   └── AlertItem.tsx
│   ├── services/         # خدمات API
│   │   └── api.ts
│   ├── navigation/       # إعدادات التنقل
│   │   └── AppNavigator.tsx
│   └── utils/           # وظائف مساعدة
│       ├── colors.ts
│       └── helpers.ts
├── android/             # ملفات Android Native
├── ios/                 # ملفات iOS Native
├── App.tsx             # نقطة الدخول الرئيسية
├── package.json
└── README.md
```

## 🌍 الترجمة

التطبيق يدعم اللغات التالية:
- العربية (افتراضي)
- English

## 🔐 الأمان

- تخزين آمن للـ Tokens باستخدام AsyncStorage
- HTTPS فقط للاتصال بالـ API
- التحقق من الصلاحيات قبل كل طلب
- Timeout للطلبات (10 ثوانٍ)

## 📱 الميزات القادمة

- [ ] Offline Mode
- [ ] Push Notifications
- [ ] Camera Integration للصور الميدانية
- [ ] GPS Tracking
- [ ] Dark Mode
- [ ] Biometric Authentication
- [ ] Multi-language Support

## 🐛 المشاكل الشائعة

### المشكلة: "Unable to connect to API"
**الحل:** تأكد من أن API يعمل وأن `EXPO_PUBLIC_API_URL` صحيح

### المشكلة: "Maps not loading"
**الحل:** تأكد من إضافة Google Maps API Key في `.env`

### المشكلة: "Expo Go crashes"
**الحل:** امسح cache بـ `expo start -c`

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل:
- GitHub Issues: https://github.com/kafaat/sahool-project/issues
- Email: support@sahool.com

## 📄 الترخيص

MIT License - انظر ملف LICENSE للتفاصيل
